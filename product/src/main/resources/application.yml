spring:
  application:
    name: product
  datasource:
    url: jdbc:postgresql://localhost:5433/product_db
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: password
  grpc:
    server:
      port: 9092
      reflection:
        enabled: true

  jpa:
    hibernate:
      ddl-auto: create

server:
  port: 8082